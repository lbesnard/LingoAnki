import 'package:flutter/material.dart';

/// Stub for the Translation Review screen.
class TranslationReviewScreen extends StatelessWidget {
  const TranslationReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Translation Review'),
      ),
      body: const Center(
        child: Text(
          'Translation Review screen (stub)',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
